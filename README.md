# FairFace Race Classifier App 🚀

Simple app to predict race from face image using deep learning (PyTorch + ResNet34) trained on FairFace dataset.

## How to run

```bash
pip install -r requirements.txt
streamlit run app.py
